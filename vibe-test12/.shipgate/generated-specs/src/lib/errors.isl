# STATUS: INCOMPLETE — regex-fallback scaffold (inference engine unavailable)

domain Errors {
  version: "1.0.0"

  entity AppError {
    id: String
  }
  entity InvalidPriorityError {
    id: String
  }
  entity DuplicateEmailError {
    id: String
  }
  entity InvalidUsernameError {
    id: String
  }
  entity UnauthorizedError {
    id: String
  }
}
