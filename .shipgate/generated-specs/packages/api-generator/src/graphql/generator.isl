# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: generateGraphQLApi, resolvers, GraphQLOptions, GraphQLGenerator, Context, Resolvers, QueryResolvers
# dependencies: graphql

domain Generator {
  version: "1.0.0"

  type GraphQLOptions = String
  type GraphQLGenerator = String
  type Context = String
  type Resolvers = String
  type QueryResolvers = String

  invariants exports_present {
    - true
  }
}
