# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: generateServer, ServerOptions, ServerGenerator
# dependencies: express, cors, helmet, fastify, @fastify/cors, hono, @hono/node-server, hono/cors, hono/logger

domain Generator {
  version: "1.0.0"

  type ServerOptions = String
  type ServerGenerator = String

  invariants exports_present {
    - true
  }
}
