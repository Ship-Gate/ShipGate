# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: createToken, verifyToken, decodeToken, generateKeyPair, generateSecret
# dependencies: jose, uuid

domain Jwt {
  version: "1.0.0"

  invariants exports_present {
    - true
  }
}
