# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: computeScore, explainScore, formatScoreCompact, SCORING_WEIGHTS, STATUS_SCORES, DEFAULT_SHIP_THRESHOLD, NO_SHIP_RULES
# dependencies: 

domain Scoring {
  version: "1.0.0"

  invariants exports_present {
    - true
  }
}
