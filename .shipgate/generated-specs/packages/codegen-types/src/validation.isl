# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: parse, is, validate, safeParse, safeValidate, generateZodValidation, UUIDSchema, TimestampSchema, DurationSchema, DecimalSchema, MoneySchema, SchemaRegistry, ZodGenerator
# dependencies: zod

domain Validation {
  version: "1.0.0"

  type ZodGenerator = String

  invariants exports_present {
    - true
  }
}
