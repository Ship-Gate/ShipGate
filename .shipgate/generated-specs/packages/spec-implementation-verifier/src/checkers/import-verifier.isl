# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: runImportVerifier
# dependencies: path, fs/promises, typescript, ${imp.specifier}

domain ImportVerifier {
  version: "1.0.0"

  invariants exports_present {
    - true
  }
}
