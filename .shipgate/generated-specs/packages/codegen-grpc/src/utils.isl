# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: toScreamingSnakeCase, toSnakeCase, toPascalCase, toCamelCase, toProtoCase, toProtoPackage, escapeProtoString, escapeRegexForProto, indent, joinProtoLines, protoComment, protoFileHeader
# dependencies: ${imp}

domain Utils {
  version: "1.0.0"

  invariants exports_present {
    - true
  }
}
