# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: resolveGo, scanGoFile, GoResolverOptions
# dependencies: node:path, node:fs/promises, path, ) && !trimmed.includes(

domain GoResolver {
  version: "1.0.0"

  type GoResolverOptions = String

  invariants exports_present {
    - true
  }
}
