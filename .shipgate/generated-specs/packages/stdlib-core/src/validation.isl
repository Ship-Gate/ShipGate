# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: ok, fail, failMany, createError, minLength, maxLength, length, pattern, min, max, range, precision, oneOf, compose, optional, nullable, array, object, string, number, integer, boolean, date, createValidator, validateAsync, Validation, ValidationResult, ValidationError, Validator, ValidatorOptions, ValidatorBuilder, AsyncValidator
# dependencies: 

domain Validation {
  version: "1.0.0"

  type ValidationResult = String
  type ValidationError = String
  type Validator = String
  type ValidatorOptions = String
  type ValidatorBuilder = String
  type AsyncValidator = String

  invariants exports_present {
    - true
  }
}
