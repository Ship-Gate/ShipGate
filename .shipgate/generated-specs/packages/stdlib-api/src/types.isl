# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: HttpMethod, RequestConfig, ApiResponse, MiddlewareContext, NextFn, Middleware, ClientConfig
# dependencies: 

domain Types {
  version: "1.0.0"

  type HttpMethod = String
  type RequestConfig = String
  type ApiResponse = String
  type MiddlewareContext = String
  type NextFn = String
  type Middleware = String
  type ClientConfig = String

  invariants exports_present {
    - true
  }
}
