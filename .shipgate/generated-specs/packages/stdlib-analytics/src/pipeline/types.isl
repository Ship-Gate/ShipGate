# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: TransformFn, FilterFn, SinkFn, PipelineStage, PipelineConfig, PipelineStats
# dependencies: 

domain Types {
  version: "1.0.0"

  type TransformFn = String
  type FilterFn = String
  type SinkFn = String
  type PipelineStage = String
  type PipelineConfig = String
  type PipelineStats = String

  invariants exports_present {
    - true
  }
}
