# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: createProjection, ProjectionHandler, Projection, ProjectionDefinition, ProjectionBuilder
# dependencies: 

domain Projection {
  version: "1.0.0"

  type ProjectionHandler = String
  type Projection = String
  type ProjectionDefinition = String
  type ProjectionBuilder = String

  invariants exports_present {
    - true
  }
}
