# STATUS: INCOMPLETE — regex-fallback scaffold (inference engine unavailable)

domain ViteEnvD {
  version: "1.0.0"

  entity ImportMetaEnv {
    id: String
  }
  entity ImportMeta {
    id: String
  }
}
