# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: parseImports, extractPackageName, X
# dependencies: y, ) || trimmed.startsWith(, ) && !trimmed.startsWith(, foo

domain ImportParser {
  version: "1.0.0"

  invariants exports_present {
    - true
  }
}
