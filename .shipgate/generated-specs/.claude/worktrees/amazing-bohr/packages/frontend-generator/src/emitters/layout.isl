# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: emitSidebar, Sidebar, emitHeader, Header, emitShell, Shell, screensToNavItems, NavItem, SidebarProps, HeaderProps, ShellProps
# dependencies: next/link, @/lib/utils, @/components/ui/button, @/components/ui/dropdown-menu, @/components/ui/avatar, react

domain Layout {
  version: "1.0.0"

  type NavItem = String
  type SidebarProps = String
  type HeaderProps = String
  type ShellProps = String

  invariants exports_present {
    - true
  }
}
