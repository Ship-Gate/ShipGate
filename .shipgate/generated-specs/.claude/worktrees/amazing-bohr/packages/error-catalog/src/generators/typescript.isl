# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: is, createError, fromResponse, getErrorMetadata, isRetriable, getErrorCodesForDomain, ErrorMap, TypeScriptGenerator, ErrorResponse, ErrorMetadata, ErrorDetails
# dependencies: 

domain Typescript {
  version: "1.0.0"

  type TypeScriptGenerator = String
  type ErrorResponse = String
  type ErrorMetadata = String
  type ErrorDetails = String

  invariants exports_present {
    - true
  }
}
