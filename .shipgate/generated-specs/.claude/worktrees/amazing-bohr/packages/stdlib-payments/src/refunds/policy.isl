# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: RefundPolicyEngine, RefundContext
# dependencies: 

domain Policy {
  version: "1.0.0"

  type RefundPolicyEngine = String
  type RefundContext = String

  invariants exports_present {
    - true
  }
}
