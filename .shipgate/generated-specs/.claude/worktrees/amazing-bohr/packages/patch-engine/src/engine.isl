# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: applyPatches, preflightPatches, PatchEngine
# dependencies: diff, ) || line.startsWith(

domain Engine {
  version: "1.0.0"

  type PatchEngine = String

  invariants exports_present {
    - true
  }
}
