# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: StorageConfig, StorageMetrics, StorageEvents, Serializer, SerializationOptions, CacheConfig, BatchOperation, BatchResult, BatchOptions, Transaction, TransactionOptions, HealthCheckResult, StorageProvider, MemoryStoreConfig, RedisStoreConfig, StorageFactory, StorageUtils
# dependencies: 

domain Types {
  version: "1.0.0"

  type StorageConfig = String
  type StorageMetrics = String
  type StorageEvents = String
  type Serializer = String
  type SerializationOptions = String
  type CacheConfig = String
  type BatchOperation = String
  type BatchResult = String
  type BatchOptions = String
  type Transaction = String
  type TransactionOptions = String
  type HealthCheckResult = String
  type StorageProvider = String
  type MemoryStoreConfig = String
  type RedisStoreConfig = String
  type StorageFactory = String
  type StorageUtils = String

  invariants exports_present {
    - true
  }
}
