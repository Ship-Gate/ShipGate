# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: FindingSeverity, Finding, SpecRoute, SpecEntity, SpecBehavior, InferredSpec, VerificationContext, VerifierChecker
# dependencies: 

domain Types {
  version: "1.0.0"

  type FindingSeverity = String
  type Finding = String
  type SpecRoute = String
  type SpecEntity = String
  type SpecBehavior = String
  type InferredSpec = String
  type VerificationContext = String
  type VerifierChecker = String

  invariants exports_present {
    - true
  }
}
