# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: createMixpanelProvider, MixpanelConfig, MixpanelProvider
# dependencies: 

domain Mixpanel {
  version: "1.0.0"

  type MixpanelConfig = String
  type MixpanelProvider = String

  invariants exports_present {
    - true
  }
}
