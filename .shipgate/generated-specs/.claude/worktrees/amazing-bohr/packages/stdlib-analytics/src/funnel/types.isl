# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: FunnelStep, EventPropertyFilter, FilterOperator, FunnelResult, FunnelStepResult, FunnelConfig, FunnelEvent
# dependencies: 

domain Types {
  version: "1.0.0"

  type FunnelStep = String
  type EventPropertyFilter = String
  type FilterOperator = String
  type FunnelResult = String
  type FunnelStepResult = String
  type FunnelConfig = String
  type FunnelEvent = String

  invariants exports_present {
    - true
  }
}
