# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: formatScore, formatVerdict, getVerdictEmoji, formatDuration, formatRelativeTime, formatPercentage, formatPercentageWithColor, truncate, escapeMarkdown, formatCodeBlock, formatInlineCode, formatList, formatNumber, formatBytes, formatError, formatErrors, formatDelta, formatPercentageDelta
# dependencies: 

domain Format {
  version: "1.0.0"

  invariants exports_present {
    - true
  }
}
