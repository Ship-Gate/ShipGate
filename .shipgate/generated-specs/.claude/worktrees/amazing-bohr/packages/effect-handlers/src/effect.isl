# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: pure, perform, flatMap, map, ap, sequence, traverse, forEach, replicate, when, unless, Do, liftA, liftA2, liftA3, fromPromise, fromAsync, AsyncEffect
# dependencies: 

domain Effect {
  version: "1.0.0"

  type AsyncEffect = String

  invariants exports_present {
    - true
  }
}
