# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: match, multiMatch, queryString, term, terms, range, exists, prefix, wildcard, fuzzy, regexp, bool, boosting, constantScore, disMax, geoDistance, geoBoundingBox, knn, matchAll, and, or, not, filter
# dependencies: 

domain Queries {
  version: "1.0.0"

  invariants exports_present {
    - true
  }
}
