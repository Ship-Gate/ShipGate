# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: invalidInput, invalidTimestamp, duplicateEntry, storageError, rateLimited, invalidQuery, invalidDateRange, queryTimeout, ruleEvaluationFailed, invalidRule, reportGenerationFailed, purgeFailed, invalidPolicy, policyConflict
# dependencies: 

domain Errors {
  version: "1.0.0"

  invariants exports_present {
    - true
  }
}
