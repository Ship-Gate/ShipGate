# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: BackpressureController, BackpressureStrategy, BackpressureAction, BackpressureOptions, BackpressureResult, BackpressureMetrics, BackpressureState, BackpressureEvent, BackpressureConfig, ThrottleConfig, BlockConfig, RedirectConfig, DeadLetterConfig
# dependencies: 

domain Types {
  version: "1.0.0"

  type BackpressureController = String
  type BackpressureStrategy = String
  type BackpressureAction = String
  type BackpressureOptions = String
  type BackpressureResult = String
  type BackpressureMetrics = String
  type BackpressureState = String
  type BackpressureEvent = String
  type BackpressureConfig = String
  type ThrottleConfig = String
  type BlockConfig = String
  type RedirectConfig = String
  type DeadLetterConfig = String

  invariants exports_present {
    - true
  }
}
