# @tier 3 — Auto-generated utility spec (export-only verification)
# exports: escapeHtml, formatDate, formatDuration, generateStyles, renderBadge, renderScoreCard, renderClauseItem, renderClauseList, renderAssumptionItem, renderAssumptions, renderQuestionItem, renderOpenQuestions, renderArtifactItem, renderArtifacts, renderHeader, renderFooter, wrapInDocument
# dependencies: 

domain Templates {
  version: "1.0.0"

  invariants exports_present {
    - true
  }
}
