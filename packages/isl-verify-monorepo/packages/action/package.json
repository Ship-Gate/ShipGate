{
  "name": "@shipgate/action",
  "version": "1.0.0",
  "description": "ISL Verify GitHub Action with PR comments (License Required)",
  "license": "SEE LICENSE IN LICENSE",
  "main": "./dist/index.js",
  "files": [
    "dist",
    "action.yml",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -b && ncc build dist/index.js -o dist-bundle",
    "clean": "rm -rf dist dist-bundle tsconfig.tsbuildinfo",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@shipgate/core": "workspace:*",
    "@shipgate/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3",
    "vitest": "^1.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
