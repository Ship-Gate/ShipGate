// BAD: Minimal exports — auto-generated spec will have very low coverage
// Only one trivial function with no types, no validation, no error handling

export function add(a: number, b: number): number {
  return a + b;
}
