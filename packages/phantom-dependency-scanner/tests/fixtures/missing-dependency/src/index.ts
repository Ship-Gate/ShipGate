import { debounce } from 'lodash';
import express from 'express'; // Missing dependency
import react from 'react'; // Missing dependency
import x from 'lodahs'; // Typo of lodash -> should get suggestion

export function main() {
  return { express, react, x };
}
