[package]
name = "myapp"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = "1.0"
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.11", optional = true }
toml = "0.8"

[dev-dependencies]
criterion = "0.5"
