# Connect GitHub

Link your repo to see PRs and workflow runs in the sidebar.

1. Add a GitHub token: `shipgate.github.token` in settings
2. Click **Connect** in the Shipgate panel

[Configure GitHub Token](command:workbench.action.openSettings?%5B%22shipgate.github.token%22%5D)

Fine-grained tokens with `repo` scope work best.
