/**
 * Auth stub - verifyAuth for protected routes
 * Replace with your actual auth (e.g. NextAuth, Clerk)
 */
export function verifyAuth(_request: Request): string {
  return 'user-id-placeholder';
}
