// Auto-generated by @isl-lang/ui-generator — DO NOT EDIT

export function validateUser(values: Record<string, unknown>): Record<string, string> {
  const errors: Record<string, string> = {};
  if (values['email'] == null || values['email'] === '') {
    errors['email'] = "Email is required";
  }
  if (values['email'] != null && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(String(values['email']))) {
    errors['email'] = "Email must be a valid email";
  }
  if (values['status'] == null || values['status'] === '') {
    errors['status'] = "Status is required";
  }
  return errors;
}

export function validateSession(values: Record<string, unknown>): Record<string, string> {
  const errors: Record<string, string> = {};
  if (values['expires_at'] == null || values['expires_at'] === '') {
    errors['expires_at'] = "Expires At is required";
  }
  if (values['revoked'] == null || values['revoked'] === '') {
    errors['revoked'] = "Revoked is required";
  }
  if (values['ip_address'] == null || values['ip_address'] === '') {
    errors['ip_address'] = "Ip Address is required";
  }
  return errors;
}

export function validateLogin(values: Record<string, unknown>): Record<string, string> {
  const errors: Record<string, string> = {};
  if (values['email'] == null || values['email'] === '') {
    errors['email'] = "Email is required";
  }
  if (values['email'] != null && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(String(values['email']))) {
    errors['email'] = "Email must be a valid email";
  }
  if (values['password'] == null || values['password'] === '') {
    errors['password'] = "Password is required";
  }
  if (values['ip_address'] == null || values['ip_address'] === '') {
    errors['ip_address'] = "Ip Address is required";
  }
  // Custom: email.is_valid — Email must be valid
  if (String(values['password'] ?? '').length < 8) {
    errors['password'] = "Password must be at least 8 characters";
  }
  return errors;
}

export function validateRegister(values: Record<string, unknown>): Record<string, string> {
  const errors: Record<string, string> = {};
  if (values['email'] == null || values['email'] === '') {
    errors['email'] = "Email is required";
  }
  if (values['email'] != null && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(String(values['email']))) {
    errors['email'] = "Email must be a valid email";
  }
  if (values['password'] == null || values['password'] === '') {
    errors['password'] = "Password is required";
  }
  if (values['confirm_password'] == null || values['confirm_password'] === '') {
    errors['confirm_password'] = "Confirm Password is required";
  }
  // Custom: email.is_valid — Email must be valid
  if (String(values['password'] ?? '').length < 8) {
    errors['password'] = "Password must be at least 8 characters";
  }
  if (values['password'] !== values['confirm_password']) {
    errors['password'] = "Password must match Confirm Password";
  }
  return errors;
}
