# Smoke Fresh Fixture

Minimal spec + impl pair for the ShipGate golden path smoke test.

- `spec.isl` — UserService domain (CreateUser, GetUser)
- `impl.ts` — Compliant TypeScript implementation

Used by `scripts/smoke-fresh.sh` and `scripts/smoke-fresh.ps1` for verify and gate steps.
